version: 0.1

phases:
  install:
    commands:
      - echo installing nodejs...
#      - curl -sL https://deb.nodesource.com/setup_12.x | bash -
#      - apt-get install -y nodejs  #aws code build use ubuntu environement
      - n 18
  pre_build:
    commands:
      - echo installing dependencies...
      - npm i -g @angular/cli
      - npm install
  build:
    commands:
      # - echo testing...
      # - echo building...
      - ng build --configuration production
  post_build:
    commands:
      - mv appspec.yml ./dist/TestUI
      - mv scripts ./dist/TestUI
artifacts:
  files:
    - "**/*"
  discard-paths: no
  base-directory: dist/TestUI
